{
  "name": "unleaded",
  "version": "1.0.0",
  "description": "ulcomm design",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i css/style.css -o css/main.css",
    "buildwatch": "npx tailwindcss -i css/style.css -o css/main.css --watch"
  },
  "author": "Ryan Jones",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  }
}
